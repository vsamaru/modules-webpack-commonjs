{
"module": "dist/i.mjs",
  
  "scripts": {
    "g": "git add . && git commit -m \"first commit\" && git push -u origin main -f",
    "dev": "npx wrangler pages dev ./public --kv URLS"
  },
  "dependencies": {
  }
}
